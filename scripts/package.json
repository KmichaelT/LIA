{
  "name": "zeffy-webhook-proxy",
  "version": "1.0.0",
  "description": "Simple webhook proxy for Zeffy to Strapi integration",
  "main": "webhook-proxy.js",
  "scripts": {
    "start": "node webhook-proxy.js",
    "dev": "nodemon webhook-proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16"
  }
}